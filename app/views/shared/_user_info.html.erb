<br>
<div class="center">
    <%= link_to gravatar_for(current_user, size: 70), current_user %>
    <h1><%= current_user.Fname %></h1>
    <span><%= link_to "View my profile", current_user %></span>
    <span>&nbsp;-&nbsp</span>
    <span><%= pluralize(current_user.microposts.count, "micropost") %></span>
</div>